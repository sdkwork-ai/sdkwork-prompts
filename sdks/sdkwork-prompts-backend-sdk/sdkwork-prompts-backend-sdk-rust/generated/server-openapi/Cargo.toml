[workspace]

[package]
name = "sdkwork-prompts-backend-sdk"
version = "0.1.1"
edition = "2021"
description = "sdkwork-prompts-backend-sdk SDK"
license = "MIT"
authors = ["SDKWork Team"]

[lib]
name = "sdkwork_prompts_backend_sdk"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.13", default-features = false, features = ["form", "json", "multipart", "query", "rustls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
http = "1.0"

[dev-dependencies]
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
