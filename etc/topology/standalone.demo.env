# standalone.demo — independent demonstration deployment, fully isolated from dev/test/staging/prod.
# Provisioned as its own deployment: dedicated PostgreSQL database/schema `sdkwork_prompts_demo` and a
# dedicated Redis logical DB index so no shared-redis cache key (rate-limit, session, idempotency)
# ever collides with another shared-redis environment.
SDKWORK_PROMPTS_DEPLOYMENT_PROFILE=standalone
SDKWORK_PROMPTS_ENVIRONMENT=demo
SDKWORK_ENV=demo
SDKWORK_PROMPTS_PROFILE_ID=standalone.demo
VITE_SDKWORK_PROMPTS_DEPLOYMENT_PROFILE=standalone

# Region deployment dimension (REGION_SPEC.md): orthogonal to deployment profile and environment.
SDKWORK_PROMPTS_REGION_CODE=cn
SDKWORK_DATABASE_SEED_LOCALE=zh-CN

# Dedicated demo ingress bind/ports (kept apart from development 8080) so a meaningfully distinct
# origin can be published externally for system demonstrations.
SDKWORK_PROMPTS_APPLICATION_PUBLIC_INGRESS_BIND=0.0.0.0:18079
SDKWORK_PROMPTS_APPLICATION_PUBLIC_HTTP_URL=http://prompts-demo.sdkwork.com:18079
VITE_SDKWORK_PROMPTS_APPLICATION_PUBLIC_HTTP_URL=http://prompts-demo.sdkwork.com:18079

# Redis isolation: point the demo at a dedicated logical DB index on the shared Redis so hardcoded
# service prefixes remain confined to this environment and can never collide with dev/test/staging/prod.
SDKWORK_PROMPTS_REDIS_URL=redis://127.0.0.1:6379/15
SDKWORK_PROMPTS_REDIS_ENABLED=true
